/**
 * main - 2. I sometimes suffer from insomnia. And when I can't fall asleep,
 * I play what I call the alphabet
 * Return: 0
 */
#include <stdio.h>
int main(void)
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
		putchar (x);
	putchar('\n');
	return (0);
}
